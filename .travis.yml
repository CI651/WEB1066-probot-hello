sudo: required
services:
- docker

before_install:
- docker pull node:8-onbuild

install:
- docker build -t probot-hello .

script:
- docker run --rm probot-hello npm run test
- docker run --rm probot-hello npm run lint

deploy:
  provider: heroku
  api_key:
     secure:
     e3866ca0-626d-44e3-bbf4-da37743fe283
  on: heroku_production
  app: probot-tasmiya
